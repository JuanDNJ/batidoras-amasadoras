---
import HeaderLink from "./HeaderLink.astro";

import { getCollection } from "astro:content";
const marcas = await getCollection("batidora-de-pedestal");
const marcasD = marcas.map((item) => {
  if (!item.slug.includes("/")) {
    return item;
  }
});
---

<nav
  class="w-full max-h-96 flex text-stone-200 bg-stone-600 md:py-4 relative md:static z-10"
>
  <section class="w-full md:max-w-7xl m-auto grid grid-cols-6 items-center">
    <section class="flex pl-4">
      <img
        src="/images/logos/webp/batidoras-pedestal.webp"
        alt=""
        width="32"
        height="32"
      />
    </section>
    <section
      class="col-span-5 text-slate-200 flex items-start justify-end md:hidden pr-4"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 24 24"
        fill="currentColor"
        class="toggle__menubars w-12 h-12"
      >
        <path
          fill-rule="evenodd"
          d="M3 5.25a.75.75 0 0 1 .75-.75h16.5a.75.75 0 0 1 0 1.5H3.75A.75.75 0 0 1 3 5.25Zm0 4.5A.75.75 0 0 1 3.75 9h16.5a.75.75 0 0 1 0 1.5H3.75A.75.75 0 0 1 3 9.75Zm0 4.5a.75.75 0 0 1 .75-.75h16.5a.75.75 0 0 1 0 1.5H3.75a.75.75 0 0 1-.75-.75Zm0 4.5a.75.75 0 0 1 .75-.75h16.5a.75.75 0 0 1 0 1.5H3.75a.75.75 0 0 1-.75-.75Z"
          clip-rule="evenodd"></path>
      </svg>
    </section>
    <div
      class="primary__menu hidden bg-stone-600 absolute top-[100%] p-4 right-0 w-[50vw] h-auto md:w-auto z-10 flex-col col-span-6 md:static md:flex md:flex-row md:justify-end md:col-span-5 md:pr-4 gap-4"
    >
      <section class="group flex flex-col gap-4 relative">
        <HeaderLink menu href="#">Marcas</HeaderLink>
        <article
          class="hidden lg:min-w-40 group-hover:absolute group-hover:left-[-100%] group-hover:top-0 lg:group-hover:top-0 group-hover:flex group-hover:flex-col py-0 lg:py-16"
        >
          <ul
            class="w-full h-full bg-stone-600 px-2 lg:py-4 rounded bg-in flex flex-col shadow shadow-black"
          >
            {
              marcasD.map(
                (marca) =>
                  marca && (
                    <a
                      href={`/${marca.slug}`}
                      class="py-2 px-4 text-stone-200 m-0"
                    >
                      {marca.slug.replace("-", " ").toUpperCase()}
                    </a>
                  )
              )
            }
          </ul>
        </article>
      </section>

      <HeaderLink menu href="/resenas">Reseñas</HeaderLink>
    </div>
  </section>
</nav>
